
Zoob Zib Thai – FULL COMPILED VERSION

ENV VARIABLES:
MONGO_URI=
STRIPE_SECRET=
NEXT_PUBLIC_API_URL=
NEXT_PUBLIC_STRIPE_PK=

Run backend:
cd backend && npm install && node server.js

Run frontend:
cd frontend && npm install && npm run dev
